{
  "name": "carbon-server",
  "version": "0.1.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "build": "docker build -t carbon-server .",
    "serve": "docker run -i --rm --name carbon-server-instance carbon-server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "data-uri-to-buffer": "^2.0.0",
    "micro": "^9.0.0",
    "micro-query": "^0.2.0",
    "puppeteer": "^0.11.0"
  }
}
